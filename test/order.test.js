const assert = require('assert');
const Order = require('../domain/models/order');

describe('', function () {
    context('', function () {
        it('test1', async function () {

        });
    })
});
